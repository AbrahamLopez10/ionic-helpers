/*
import { BaseUploadProgressPage } from './../../ionic-helpers/pages/upload-progress/base.upload-progress';
import { Component, ChangeDetectorRef } from '@angular/core';
import { NavController, NavParams, Events, AlertController } from 'ionic-angular';

@Component({
  selector: 'page-upload-progress',
  templateUrl: '../../ionic-helpers/pages/upload-progress/upload-progress.html'
})
export class UploadProgressPage extends BaseUploadProgressPage {
  constructor(
    public navCtrl: NavController,
    public navParams: NavParams,
    protected alertCtrl: AlertController,
    protected events: Events,
    protected changeDetector: ChangeDetectorRef,
    protected translator: Translator
  ) {
    super(translator, navParams.get('fileName'));
  }
}
*/